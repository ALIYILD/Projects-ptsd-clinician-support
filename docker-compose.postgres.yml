services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ptsd
      POSTGRES_PASSWORD: ptsd
      POSTGRES_DB: ptsd_support
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
